version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: time-report-api
    container_name: time-report-api
    ports:
      - "8082:8080"
    restart: always
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__ConexionBD=Server=sqlserver;Database=ISC_TimeReport;User=sa;Password=Zp8#kV42wLp@tR3m;TrustServerCertificate=False;
      - JWT__JWTSecretKey=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
      - Cors__AllowedOrigins=http://localhost:4200
      - EmailSettings__SmtpServer=smtp.hostinger.com
      - EmailSettings__Port=465
      - EmailSettings__SenderName=TimeReport App
      - EmailSettings__SenderEmail=siaft@siaft.net
      - EmailSettings__Username=siaft@siaft.net
      - EmailSettings__Password=Vrocoli23_
    depends_on:
      - sqlserver
    networks:
      - backend-net

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: time-report-sqlserver
    restart: always
    environment:
      SA_PASSWORD: "Zp8#kV42wLp@tR3m"
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"
    volumes:
      - sqlserver_data:/var/opt/mssql
    networks:
      - backend-net

volumes:
  sqlserver_data:

networks:
  backend-net:
    driver: bridge



